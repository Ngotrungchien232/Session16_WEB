var width = parseInt(prompt("Nhập vào giá trị width: "));

var height = parseInt(prompt("Nhập vào giá trị height: "));

var dientich = width * height;

alert("Diện tích hình chữ nhật là: " + dientich);
